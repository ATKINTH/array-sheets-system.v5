◌ัnameสๅาะไาๅัสๅะ: Sheets Bot (Single: read)

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  run:
    runs-on: ubuntu-latest

    env:
      GOOGLEพอมะๆมะมๅไำีใพีก็.ไ้ไม้ไมไ้ ไัมไ_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
      SHEET_ID: ${{ secrets.SHEET_ID }}
      SHEET_TAB: ${{ secrets.SHEET_TAB }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: ac้ ห้ ได้ ๆ้ ได้ ก็ tions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          echo "📦 Installing dependencies..."
          pip install -r requirements.txt

      - name: Validate files before run
        run: |
          echo "🔍 Checking files..."
          test -f requirements.txt || (echo "❌ Missing requirements.txt" && exit 1)
          test -f main.py || (echo "❌ Missing main.py" && exit 1)
          test -f sheets_client.py || (echo "❌ Missing sheets_client.py" && exit 1)
           เห ได้ ไ่ ไecho "✅ All files are present"

      - name: Run sheet bot
        run: |
          echo "🚀 Running Sheets Bot"
          python main.py read "$SHEET_ID" "$SHEET_TAB"
